#include<stdio.h>
#include<conio.h>
 void main()
  {
   int len, b, area;
 
   printf("\nEnter the Len of Rectangle : ");
   scanf("%d", &len);
 
   printf("\nEnter the B of Rectangle : ");
   scanf("%d", &b);
 
   area = len * b;
   printf("\nArea of Rectangle : %d", area);
 
   return (0);
}
